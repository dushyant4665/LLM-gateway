{
  "name": "rentok-llm-gateway",
  "version": "1.0.0",
  "description": "A minimal LLM Gateway backend",
  "main": "server.js",
  "scripts": {
    "start": "npx prisma migrate deploy && node server.js",
    "build": "npx prisma generate",
    "dev": "nodemon server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "groq-sdk": "^1.6.0",
    "prisma": "^5.14.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.3",
    "supertest": "^7.2.2"
  }
}
